# Badge

Component spec — machine-readable description of the Badge component,
written before implementation. This file is the upstream source for both
the code implementation and the generated Figma component (see
`docs/decisions/0011-spec-single-source-of-truth.md`).

classification: control
rationale: TODO — why this component is classified as container or control.

## Intent

TODO: what problem this component solves, when to use it vs. alternatives
(see Related components below), and any usage guidelines.

## Anatomy

<!-- one bullet per part: - <part>: <role> -->
TODO: parts that make up the component — name them so design and dev use
the same terms.

## Variants

<!-- one bullet per variant: - <name>: <prop>=<value>[, <prop>=<value>...] -->
TODO: if this system serves multiple products, note here whether all
products share these variants or specific products have exceptions.

## Sizes

<!-- one bullet per size: - <size>: height=<token>, padding=<token>, iconSize=<token>, fontSize=<token> -->
TODO: exact per-size values, expressed as tokens, not raw values.

## States

<!-- one bullet per state: - <state>: <description>, naming which tokens change (color/border/shadow) -->
TODO

## Tokens used

<!-- one bullet per binding: - <part/state>: <token> -->
TODO: every visual property (color, spacing, radius, typography) must map
to a token, never a raw value.

## Content guidelines

TODO: label length/truncation behavior, copy voice, capitalization, when
icons are required vs. optional.

## Accessibility

<!-- cover: focus visibility, contrast per variant, minimum touch-target
size, ARIA-label requirement for icon-only instances, keyboard behavior -->
TODO

## Responsive behavior

TODO

## Props

<!-- one bullet per prop: - <propName>: <type> = <default>[, figma=<FigmaPropertyName> if it differs] -->
TODO: prop names, types, defaults — kept 1:1 with the Figma properties
this spec generates.

## Do's and don'ts

TODO: examples of misuse.

## Related components

TODO: note where the boundary lies against similar components.
