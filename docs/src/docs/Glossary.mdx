import { Meta } from '@storybook/addon-docs/blocks';
import { GlossaryPage } from './Glossary';

<Meta title="Docs Overview/Glossary" />

# Glossary

A curated subset of naming decisions from `docs/glossary.yaml` — the
full file stays the lint-enforced source of truth (`npm run
lint:glossary`, ADR 0002). Which terms appear here is picked by hand in
`docs/src/docs/glossary-storybook.yaml`.

<GlossaryPage />
