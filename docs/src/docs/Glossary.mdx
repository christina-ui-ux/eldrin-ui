import { Meta, Source } from '@storybook/addon-docs/blocks';
import glossaryRaw from '../../glossary.yaml?raw';

<Meta title="Glossary" />

# Glossary

Single source of truth for naming decisions across the whole system —
read directly from `docs/glossary.yaml`, so this can't drift from the
file that `npm run lint:glossary` actually enforces (see ADR 0002 in
Architecture Decisions).

<Source code={glossaryRaw} language="yaml" dark />
