# Kitchen sink

Every component, rendered with no props, in one place. Not a real scenario —
this is the baseline sanity check that `eldrin-ui` imports and renders at
all as components grow implementations. Not where real prototyping
findings belong; add a new prototype under `prototypes/<name>/` for that.
